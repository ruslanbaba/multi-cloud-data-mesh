version: 2

models:
  - name: stg_labs
    columns:
      - name: patient_id
        tests:
          - relationships:
              to: ref('stg_admissions')
              field: patient_id

  - name: patient_360
    columns:
      - name: patient_id
        tests: [not_null]
