version: 2

models:
  - name: stg_pharmacy
    columns:
      - name: patient_id
        tests: [not_null]
      - name: ndc_code
        tests: [not_null]
      - name: status
        tests:
          - accepted_values:
              values: ["active", "inactive", "cancelled"]
              quote: true
